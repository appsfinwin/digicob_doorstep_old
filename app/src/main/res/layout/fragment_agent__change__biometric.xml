<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="#f5f5f5"
    android:layout_height="match_parent"
    tools:context="com.finwingway.digicob.Agent_Change_Biometric">
    <TextView
        android:layout_marginTop="30dp"
        android:layout_centerHorizontal="true"
        android:text="Change Biometric"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <LinearLayout
        android:orientation="vertical"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <Button
            android:id="@+id/change_bio_btn"
            android:text="Click Here to chane your fingerprint"
            android:background="?attr/selectableItemBackground"
            android:drawableTop="@drawable/biometric_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <ImageButton
        android:id="@+id/change_bio_next_btn"
        android:background="?attr/selectableItemBackground"
        android:layout_margin="20dp"
        android:src="@drawable/finish_btn_bg"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <ImageButton
    android:id="@+id/change_bio_exit_btn"
    android:background="?attr/selectableItemBackground"
    android:layout_margin="20dp"
    android:src="@drawable/exit_btn_bg"
    android:layout_alignParentRight="true"
    android:layout_alignParentBottom="true"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" />
    <ImageButton
        android:id="@+id/change_bio_tryagain_btn"
        android:background="?attr/selectableItemBackground"
        android:layout_margin="20dp"
        android:src="@drawable/try_again_btn_bg"
        android:layout_alignParentLeft="true"
        android:layout_alignParentBottom="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
</RelativeLayout>
